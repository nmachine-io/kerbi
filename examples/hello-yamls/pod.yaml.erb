apiVersion: v1
kind: Pod
metadata:
  name: "hello-kerbi"
  namespace: <%= release_name %>
  annotations: <%= embed(file('annotations'), 3) %>
spec:
  containers:
    - name: main
      image: <%= values.dig(:container, :image) %>
      command: <%= values.dig(:container, :command) %>